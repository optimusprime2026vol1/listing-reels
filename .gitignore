__pycache__/
*.pyc
.venv/
venv/
output/
renders/
*.mp4
.DS_Store

# assets/photos, assets/music, assets/voiceover are intentionally NOT
# ignored -- commit real listing inputs here so the GitHub Actions
# workflow can pick them up and build in the cloud.
